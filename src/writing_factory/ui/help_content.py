"""Single source of truth for page help dialogs and the full tutorial."""

from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True, slots=True)
class HelpTopic:
    """One user-facing help chapter."""

    key: str
    title: str
    markdown: str
    allow_external_links: bool = False


HELP_TOPICS: dict[str, HelpTopic] = {
    "project": HelpTopic(
        "project",
        "项目",
        """## 功能

项目用于归集同一写作工作的任务记录。它不复制知识库，也不会改变作者档案。

## 操作

1. 输入名称和说明，点击“新建”。
2. 选择已有项目后可修改名称和说明，再点击“保存”。
3. 支持复选框或 Ctrl/Shift 多选后批量删除。

删除项目会同时删除其写作任务记录，但不会删除知识库文档或作者档案。
""",
    ),
    "knowledge": HelpTopic(
        "knowledge",
        "知识库",
        """## 功能

知识库是本地、长期保存的事实与证据来源。文档入库后会保留页码、章节和切片定位，
并建立向量与 BM25 双索引。作者对话和写作任务还可按次启用博查联网检索；
联网结果不会自动写入知识库。

## 操作

1. 点击“批量导入”选择 PDF、Word、PPT 或 UTF-8 TXT。
2. 等待状态变为“可检索”。MinerU 文档按顺序解析，避免无节制占用额度。
3. 使用页面下方检索面板检查关键词或问题能否命中预期文档。
4. 支持复选框或 Ctrl/Shift 多选删除。

删除文档会同步清理其切片和索引。正在被长期任务引用的来源卡片仍保留当时的审计快照。
""",
    ),
    "persona": HelpTopic(
        "persona",
        "作者档案",
        """## 功能

作者档案从目标语料提炼心智模型、谋篇 DNA 和表达 DNA。它只规定怎样思考和表达，不是事实来源。

## 操作

1. 输入档案名称，选择人物或主题模式。
2. 勾选目标语料；需要判断作者区分度时，再勾选同领域或同文体对照语料并填写内容领域。
3. 选择快速、均衡、深度或自定义质量模式；点击“？”可查看各步骤的预计成本和关闭影响。
4. 点击“蒸馏”创建全新档案，在实时输出区选择具体 Map 或 Reduce 调用查看。同名档案不会被隐式续跑。
5. 未完成档案用“继续”恢复所选版本的精确断点；已经完成的档案用“升级”加入新语料或重新构建新版本。
6. 完成后双击档案查看审计 JSON、Markdown、运行时投影和版本历史。
7. “自检”依次执行设计问题、档案盲测回答和中性独立评判，会额外消耗 SiliconFlow 额度。
   每阶段成功后自动保存断点；失败后重新选中档案并点击“继续自检”，会跳过已经完成的阶段。
   “未检 · 可续 2/3”表示已有两个阶段可以直接复用。

## 语料数量建议

- 1–2 篇通常只能形成暂定观察；3–5 篇可以建立初步档案；6–12 篇代表性目标语料通常最能兼顾质量与效率。
- 每个需要提炼谋篇 DNA 的文体最好至少提供 3 篇。
- 若要覆盖多个主题、时期或文体，应分层选择代表作，不要只增加高度重复的文本。
- 对照语料建议选择 4–8 篇同领域、同文体、非目标作者文本，用于区分作者特征与通用写作惯例。
- 超过 12 篇后通常进入收益递减区。可先完成一版，再每次加入 2–5 篇新语料进行升级。
- 当核心模型和谋篇规律不再明显变化时，档案已接近稳定。
- 文档篇幅也影响耗时：基础 Map 随切片数量增长，文档画像和谋篇分析随文档数增长。
- 并发只缩短等待，不减少 API 调用或额度。

升级失败不会覆盖上一个可用版本。目标作者语料默认不会在新写作或作者对话中充当事实来源。
停止任务后，已经完成的文档级断点会保留。
""",
    ),
    "author_chat": HelpTopic(
        "author_chat",
        "作者对话",
        """## 功能

作者对话使用无证据的作者运行时档案进行探索性交流，不代表真人，也不等同于经过完整核验的正式成稿。

## 操作

1. 新建会话并选择一个已经完成的作者档案。
2. 选择“通用知识辅助”或“严格证据”：前者保留模型的一般知识能力，后者要求具体事实来自知识库。
3. 可选择不使用知识库、使用全部可用文档或只使用勾选文档。
4. 需要当前公开网页信息时，可勾选“使用博查联网检索”；它可与本地知识库同时使用，也可单独使用。
5. 发送消息后会流式显示回答；停止时保留已经收到的部分。
6. 历史会话自动保存。系统上传滚动摘要和设置中指定的最近 N 轮。
7. 有本地或联网来源的回答可点击“核验本条”运行中性事实检查。

作者档案是认知与表达增量，不是知识上限。通用知识辅助不会伪造作者经历或真实立场，时效性与高风险事实仍需知识库核验。历史摘要只是对话记忆，不是事实证据。作者目标语料仍默认隔离，确需检索原文时必须明确允许。
""",
    ),
    "writing": HelpTopic(
        "writing",
        "写作任务",
        """## 功能

写作任务使用作者档案完成创作意图、内容规划、逐单元起草、核验和打磨。既可结合知识库写事实稿，也可在没有事实语料时生成无事实构思稿。

## 操作

1. 选择项目和作者档案，再选择“知识库事实写作”或“无事实构思”。
   事实写作需要本地语料或博查联网检索；无事实构思可以使用 0 篇语料。
2. 描述文体、受众、目的和篇幅；也可手动选择非虚构文体与引用呈现方式。
3. 需要公开网页材料时勾选“使用博查联网检索”。未勾选时不会产生博查请求，也不会改变原有本地流程。
4. 选择质量模式或自定义质量步骤后开始写作。
5. 在“内容单元与输出”中查看进度、实时模型输出和调用明细。
6. 失败或停止后选择原任务并点击“继续所选”，已经完成的内容单元不会从头生成。

事实写作中的论断必须绑定本地文档或联网网页来源。联网摘要会保存标题、URL、站点与发布时间，并和本地证据一样先冻结、再核验、最后由代码生成引用。无事实构思不检索知识库或网络、不生成参考文献，只允许观点、问题、框架、建议和条件性假设，并强制执行不可关闭的外部事实混入检查。关闭普通事实核验或防漂移步骤时，事实稿会标为未核验草稿。
""",
    ),
    "settings": HelpTopic(
        "settings",
        "设置",
        """## 功能

设置页管理 SiliconFlow、MinerU 与博查 API 凭据、模型选择、统一并发与超时，
以及每个 SiliconFlow 文字生成步骤的独立参数。

## 操作

1. 配置 SiliconFlow 与 MinerU 凭据；需要联网搜索时再配置博查。
   密钥保存在本机凭据库而不是普通配置文件。
2. 联网刷新并选择文字生成、Embedding 和 Rerank 模型。
3. 在步骤表中分别设置温度、思考模式、输出上限、流式请求、重试、单次尝试上限和整项调用总预算。
4. “作者对话最近轮数”决定每次对话原样上传多少轮，较早内容由后台摘要承接。
5. “每次联网检索条目数”由作者对话和写作任务共用，范围 1–20；
   设置的是每一次搜索请求，不是整个长任务的总数。

“单次尝试上限”会完整分配给每一次流式或非流式请求；“整项调用总预算”只决定是否允许启动下一次尝试，
不会截断已经启动且仍在输出的请求。“流式空闲超时”从最后一个有效数据块重新计时，持续有输出时不会触发。
完整流式结果必须收到结束标志并通过结构校验；失败尝试的残片不会进入正式缓存。

Embedding 模型变化需要重建向量索引；界面会在切换前明确显示待重建状态。
""",
    ),
    "credentials": HelpTopic(
        "credentials",
        "API 获取与配置",
        """## SiliconFlow API

[使用邀请链接访问 SiliconFlow 官网](https://cloud.siliconflow.cn/i/j7F36Uco)

通过这个邀请链接注册可以获得免费额度。

1. 注册并登录后，点击左侧菜单中的“API 密钥”。
2. 点击“新建 API 密钥”。
3. 为密钥填写一个便于识别的名称，例如“我的模型软件”，然后创建。
4. 复制页面显示的、以 `sk-` 开头的密钥。
5. 返回 Parrofish 设置页，在 SiliconFlow 一行点击“编辑”，粘贴密钥并点击“保存”。

## MinerU API

[访问 MinerU 官网](https://mineru.net/)

1. 点击网页右上角的“登录/注册”并完成登录。
2. 点击页面上方的“API”。
3. 在左侧进入“API 管理”。
4. 点击“创建 Token”，创建后立即复制并妥善保存。
5. 返回 Parrofish 设置页，在 MinerU 一行点击“编辑”，粘贴 Token 并点击“保存”。

## 博查 Web Search API

[访问博查 AI 开放平台](https://open.bochaai.com/)

1. 注册并登录博查 AI 开放平台，进入控制台。
2. 在控制台创建 API Key，并立即复制保存。
3. 返回 Parrofish 设置页，在“博查搜索”一行点击“编辑”。
4. 粘贴 API Key，Base URL 保持 `https://api.bochaai.com/v1`，点击“保存”。
5. 在运行规则中设置“每次联网检索条目数”；只有任务中明确勾选联网时才会调用并消耗额度。

## 密钥安全

API 密钥和 Token 相当于账号密码。不要发送给他人，不要粘贴到聊天、日志、截图或公开仓库中。
Parrofish 会把在设置页保存的凭据写入本机凭据库，不会写入普通项目配置文件。
""",
        allow_external_links=True,
    ),
}

TUTORIAL_ORDER = (
    "quick_start",
    "project",
    "knowledge",
    "persona",
    "author_chat",
    "writing",
    "settings",
    "safety",
)

TUTORIAL_TOPICS: dict[str, HelpTopic] = {
    "quick_start": HelpTopic(
        "quick_start",
        "快速开始",
        """## 推荐顺序

1. 需要事实写作时，可在知识库导入长期资料，也可在设置中配置博查后按任务启用联网检索；
   只做观点或框架构思时两者都可跳过。
2. 在作者档案选择目标语料并完成蒸馏。
3. 用作者对话探索问题，或在写作任务创建可恢复的正式写作任务。
4. 在设置页按需要调整模型、并发、超时和各步骤质量参数。

本地知识库与可选联网结果回答“写什么事实”，作者档案回答“怎样思考和表达”。这些通道始终分开。
""",
    ),
    **HELP_TOPICS,
    "safety": HelpTopic(
        "safety",
        "事实、安全与恢复",
        """## 事实边界

- 作者档案及聊天历史不能充当事实来源。
- 正式写作先冻结本地或联网证据，再起草、核验和施加文风。
- 来源键由代码绑定，参考文献由代码拼装。

## 提示注入

知识库文本、联网摘要和历史消息都被视为不可信数据。博查搜索只在用户勾选后由程序执行；生成模型只能读取返回结果，没有自行联网、执行代码或操作外部系统的工具。

## 断点恢复

蒸馏 Map、结构画像、写作内容单元和聊天消息都会逐步持久化。
安全停止后可继续任务；强制结束进程可能留下标记为中断的最后一条输出，
但不会删除已经完成的断点。
""",
    ),
}
