"""Single source of truth for page help dialogs and the full tutorial."""

from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True, slots=True)
class HelpTopic:
    """One user-facing help chapter."""

    key: str
    title: str
    markdown: str
    allow_external_links: bool = False


HELP_TOPICS: dict[str, HelpTopic] = {
    "project": HelpTopic(
        "project",
        "项目",
        """## 功能

项目用于归集同一写作工作的任务记录。它不复制知识库，也不会改变作者档案。

## 操作

1. 输入名称和说明，点击“新建”。
2. 选择已有项目后可修改名称和说明，再点击“保存”。
3. 支持复选框或 Ctrl/Shift 多选后批量删除。

删除项目会同时删除其写作任务记录，但不会删除知识库文档或作者档案。
""",
    ),
    "knowledge": HelpTopic(
        "knowledge",
        "知识库",
        """## 功能

知识库是事实、证据和引用的唯一来源。文档入库后会保留页码、章节和切片定位，并建立向量与 BM25 双索引。

## 操作

1. 点击“批量导入”选择 PDF、Word、PPT 或 UTF-8 TXT。
2. 等待状态变为“可检索”。MinerU 文档按顺序解析，避免无节制占用额度。
3. 使用页面下方检索面板检查关键词或问题能否命中预期文档。
4. 支持复选框或 Ctrl/Shift 多选删除。

删除文档会同步清理其切片和索引。正在被长期任务引用的来源卡片仍保留当时的审计快照。
""",
    ),
    "persona": HelpTopic(
        "persona",
        "作者档案",
        """## 功能

作者档案从目标语料提炼心智模型、谋篇 DNA 和表达 DNA。它只规定怎样思考和表达，不是事实来源。

## 操作

1. 输入档案名称，选择人物或主题模式。
2. 勾选目标语料；需要判断作者区分度时，再勾选同领域或同文体对照语料并填写内容领域。
3. 点击“蒸馏”，在实时输出区选择具体 Map 或 Reduce 调用查看。
4. 完成后双击档案查看审计 JSON、Markdown、运行时投影和版本历史。
5. “自检”会额外消耗 SiliconFlow 额度，只在需要时运行。

目标作者语料默认不会在新写作或作者对话中充当事实来源。停止任务后，已经完成的断点会保留。
""",
    ),
    "author_chat": HelpTopic(
        "author_chat",
        "作者对话",
        """## 功能

作者对话使用无证据的作者运行时档案进行探索性交流，不代表真人，也不等同于经过完整核验的正式成稿。

## 操作

1. 新建会话并选择一个已经完成的作者档案。
2. 可选择不使用知识库、使用全部可用文档或只使用勾选文档。
3. 发送消息后会流式显示回答；停止时保留已经收到的部分。
4. 历史会话自动保存。系统上传滚动摘要和设置中指定的最近 N 轮。
5. 有知识库证据的回答可点击“核验本条”运行中性事实检查。

历史摘要只是对话记忆，不是事实证据。作者目标语料仍默认隔离，确需检索原文时必须明确允许。
""",
    ),
    "writing": HelpTopic(
        "writing",
        "写作任务",
        """## 功能

写作任务把作者档案的形式能力与知识库的事实证据结合，完成创作意图、内容规划、逐单元起草、核验和打磨。

## 操作

1. 选择项目、作者档案和本任务允许使用的事实语料。
2. 描述文体、受众、目的和篇幅；也可手动选择非虚构文体与引用呈现方式。
3. 选择质量模式或自定义质量步骤后开始写作。
4. 在“内容单元与输出”中查看进度、实时模型输出和调用明细。
5. 失败或停止后选择原任务并点击“继续所选”，已经完成的内容单元不会从头生成。

正文中的事实论断必须绑定真实来源。关闭事实核验或防漂移步骤时，结果会标为未核验草稿。
""",
    ),
    "settings": HelpTopic(
        "settings",
        "设置",
        """## 功能

设置页管理 API 凭据、模型选择、统一并发与超时，以及每个 SiliconFlow 文字生成步骤的独立参数。

## 操作

1. 配置 SiliconFlow 与 MinerU 凭据，密钥保存在本机凭据库而不是普通配置文件。
2. 联网刷新并选择文字生成、Embedding 和 Rerank 模型。
3. 在步骤表中分别设置温度、思考模式、输出上限、流式请求、重试和超时。
4. “作者对话最近轮数”决定每次对话原样上传多少轮，较早内容由后台摘要承接。

Embedding 模型变化需要重建向量索引；界面会在切换前明确显示待重建状态。
""",
    ),
    "credentials": HelpTopic(
        "credentials",
        "API 获取与配置",
        """## SiliconFlow API

[使用邀请链接访问 SiliconFlow 官网](https://cloud.siliconflow.cn/i/j7F36Uco)

通过这个邀请链接注册可以获得免费额度。

1. 注册并登录后，点击左侧菜单中的“API 密钥”。
2. 点击“新建 API 密钥”。
3. 为密钥填写一个便于识别的名称，例如“我的模型软件”，然后创建。
4. 复制页面显示的、以 `sk-` 开头的密钥。
5. 返回 Parrofish 设置页，在 SiliconFlow 一行点击“编辑”，粘贴密钥并点击“保存”。

## MinerU API

[访问 MinerU 官网](https://mineru.net/)

1. 点击网页右上角的“登录/注册”并完成登录。
2. 点击页面上方的“API”。
3. 在左侧进入“API 管理”。
4. 点击“创建 Token”，创建后立即复制并妥善保存。
5. 返回 Parrofish 设置页，在 MinerU 一行点击“编辑”，粘贴 Token 并点击“保存”。

## 密钥安全

API 密钥和 Token 相当于账号密码。不要发送给他人，不要粘贴到聊天、日志、截图或公开仓库中。
Parrofish 会把在设置页保存的凭据写入本机凭据库，不会写入普通项目配置文件。
""",
        allow_external_links=True,
    ),
}

TUTORIAL_ORDER = (
    "quick_start",
    "project",
    "knowledge",
    "persona",
    "author_chat",
    "writing",
    "settings",
    "safety",
)

TUTORIAL_TOPICS: dict[str, HelpTopic] = {
    "quick_start": HelpTopic(
        "quick_start",
        "快速开始",
        """## 推荐顺序

1. 在知识库批量导入事实语料，等待文档可检索。
2. 在作者档案选择目标语料并完成蒸馏。
3. 用作者对话探索问题，或在写作任务创建可恢复的正式写作任务。
4. 在设置页按需要调整模型、并发、超时和各步骤质量参数。

知识库回答“写什么事实”，作者档案回答“怎样思考和表达”。这两个通道始终分开。
""",
    ),
    **HELP_TOPICS,
    "safety": HelpTopic(
        "safety",
        "事实、安全与恢复",
        """## 事实边界

- 作者档案及聊天历史不能充当事实来源。
- 正式写作先冻结证据，再起草、核验和施加文风。
- 来源键由代码绑定，参考文献由代码拼装。

## 提示注入

知识库文本和历史消息都被视为不可信数据。模型只读这些内容，没有联网、执行代码或操作外部系统的工具。

## 断点恢复

蒸馏 Map、结构画像、写作内容单元和聊天消息都会逐步持久化。
安全停止后可继续任务；强制结束进程可能留下标记为中断的最后一条输出，
但不会删除已经完成的断点。
""",
    ),
}
